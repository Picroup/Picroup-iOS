query UsernameAvailable($username: String!) {
    searchUser(username: $username) {
        username
    }
}

query PhoneNumberAvailable($phoneNumber: String!) {
    searchUserByPhoneNumber(phoneNumber: $phoneNumber) {
        username
    }
}
