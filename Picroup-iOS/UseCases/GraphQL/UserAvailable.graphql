query UserAvailable($username: String!) {
    searchUser(username: $username) {
        username
    }
}

query UserAvailableByPhoneNumber($phoneNumber: String!) {
    searchUserByPhoneNumber(phoneNumber: $phoneNumber) {
        username
    }
}
