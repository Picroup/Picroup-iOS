query RankedMedia($rankBy: RankBy, $cursor: Float) {
    rankedMedia(rankBy: $rankBy, cursor: $cursor) {
        ...CursorMediaFragment
    }
}

query HotMediaByTags($tags: [String!]) {
    hotMediaByTags(tags: $tags) {
        ...CursorMediaFragment
    }
}
