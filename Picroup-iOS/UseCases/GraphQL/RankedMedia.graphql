query RankedMedia($rankBy: RankBy, $cursor: Float) {
    rankedMedia(rankBy: $rankBy, cursor: $cursor) {
        ...CursorMediaFragment
    }
}

query HotMediaByTag($tag: String) {
    hotMediaByTag(tag: $tag) {
        ...CursorMediaFragment
    }
}
