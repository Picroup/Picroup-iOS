query Medium($userId: ID!, $mediumId: ID!, $cursor: Float = null) {
    medium(mediumId: $mediumId) {
        _id
        endedAt
        stared(userId: $userId)
        comments(cursor: $cursor) {
            cursor
            items {
                _id
            }
        }
    }
}
