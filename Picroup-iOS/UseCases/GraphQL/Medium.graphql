query Medium($userId: ID!, $mediumId: ID! $cursor: Float) {
    medium(mediumId: $mediumId) {
        _id
        endedAt
        stared(userId: $userId)
        user { ...UserFragment }
        recommendedMedia(cursor: $cursor) {
            ...CursorMediaFragment
        }
    }
}

