query Medium($userId: ID!, $mediumId: ID!) {
    medium(mediumId: $mediumId) {
        _id
        endedAt
        stared(userId: $userId)
        user {
            _id
            username
            avatarId
        }
    }
}
