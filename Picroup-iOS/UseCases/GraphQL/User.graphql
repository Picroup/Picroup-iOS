query User($userId: ID!) {
    user(userId: $userId) {
        _id
        username
        avatarId
        followingsCount
        followersCount
        reputation
        gainedReputation
        notificationsCount
    }
}
