query User($userId: ID!) {
    user(userId: $userId) {
        _id
        username
        followingsCount
        followersCount
        reputation
        gainedReputation
        notificationsCount
    }
}
