query RandedMedia($cursor: Float = null) {
    rankedMedia(category: popular, cursor: $cursor, rankBy: thisMonth) {
        cursor
        items {
            _id
        }
    }
    user: login(username: "pingguo", password: "pingguo") {
        _id
        username
        followersCount
        followingsCount
    }
}

