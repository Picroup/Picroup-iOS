query RandedMedia {
    rankedMedia(category: popular, rankBy: thisMonth) {
        cursor
        items {
            _id
        }
    }
    user: login(username: "pingguo", password: "pingguo") {
        _id
        username
        followersCount
        followingsCount
    }
}

