query MediumComments($mediumId: ID!, $cursor: Float) {
    medium(mediumId: $mediumId) {
        comments(cursor: $cursor) {
            cursor
            items {
                _id
                userId
                content
                createdAt
            }
        }
    }
}

