query UserFollowings($userId: ID!, $followedByUserId: ID!, $cursor: Float = null) {
    user(userId: $userId) {
        followings(cursor: $cursor) {
            cursor
            items {
                followed(byUserId: $followedByUserId)
                ...UserFragment
            }
        }
    }
}




